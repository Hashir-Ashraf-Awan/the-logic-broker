# Local dev defaults — copy to .env.local and edit.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
NEXT_PUBLIC_API_URL="http://localhost:4000"

# Clerk (placeholder until session 6 wires auth)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=""
CLERK_SECRET_KEY=""
NEXT_PUBLIC_CLERK_SIGN_IN_URL="/sign-in"
NEXT_PUBLIC_CLERK_SIGN_UP_URL="/sign-up"
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL="/portal"

# Database — same value as packages/db (Server Components read Prisma directly)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/logicbroker?schema=public"

# Email (later)
RESEND_API_KEY=""
EMAIL_FROM="hello@thelogicbroker.com"
